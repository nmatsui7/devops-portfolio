groups:
  - name: portfolio-local
    rules:
      - alert: PortfolioAppDown
        expr: up{job="portfolio-app-local"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Portfolio app target is down"
          description: "Prometheus cannot scrape the local portfolio app target."

      - alert: PortfolioAppNotReady
        expr: portfolio_app_ready != 1
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "Portfolio app readiness metric is not healthy"
          description: "The local demo app is not reporting portfolio_app_ready = 1."
